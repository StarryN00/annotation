# 樟巢螟数据标注与模型训练系统 — 设计与开发说明

> **版本**: V1.0 | **日期**: 2026-03-08 | **状态**: 设计阶段
>
> **说明**: 本文档覆盖"数据标注 + 模型训练"独立项目。与生产检测系统为上下游关系，本项目的最终交付物是训练好的模型权重文件，供生产检测系统加载使用。

---

## 一、项目概述

### 1.1 项目目标

构建一套自动化工具链，将无人机航拍图片转化为可用于 YOLOv8 训练的标注数据，并完成模型训练，最终输出生产可用的模型权重文件。

### 1.2 核心流程总览

```
原始航拍图片
    │
    ▼
┌──────────────────────────────┐
│ Step 1: 数据准备              │  图片收集、筛选、归档
└────────────┬─────────────────┘
             ▼
┌──────────────────────────────┐
│ Step 2: AI自动标注            │  调用多模态大模型识别虫巢位置
│  (Claude / Kimi / GPT-4o)    │  输出 YOLO 格式标注文件
└────────────┬─────────────────┘
             ▼
┌──────────────────────────────┐
│ Step 3: 标注质量验证          │  可视化抽检 + 统计分析
└────────────┬─────────────────┘
             ▼
┌──────────────────────────────┐
│ Step 4: 数据集构建            │  划分训练/验证/测试集
└────────────┬─────────────────┘
             ▼
┌──────────────────────────────┐
│ Step 5: 模型训练              │  YOLOv8 目标检测训练
└────────────┬─────────────────┘
             ▼
┌──────────────────────────────┐
│ Step 6: 模型评估与导出        │  评估指标验证 → 导出权重
└────────────┬─────────────────┘
             ▼
        交付给生产检测系统
        ├── nest_det.pt    (虫巢检测模型)
        └── tree_seg.pt    (树种识别模型)
```

### 1.3 交付物清单

| 交付物 | 格式 | 说明 |
|--------|------|------|
| 虫巢检测模型权重 | `.pt` (PyTorch) | YOLOv8 训练好的权重 |
| 树种识别模型权重 | `.pt` (PyTorch) | DeepLabV3+ 训练好的权重 |
| YOLO标注数据集 | YOLO格式目录 | 图片 + 标注文件 + 类别文件 |
| 训练报告 | Markdown / PDF | 训练参数、评估指标、对比分析 |
| 模型使用说明 | Markdown | 输入格式、输出格式、推理参数、依赖说明 |

---

## 二、项目结构

```
nest-labeling-pipeline/
├── config/
│   ├── config.yaml              # 全局配置文件
│   └── prompts/                 # 各模型的提示词模板
│       ├── nest_detection.txt   # 虫巢检测提示词
│       └── tree_classification.txt
│
├── src/
│   ├── labeling/                # Step 2: 自动标注模块
│   │   ├── __init__.py
│   │   ├── labeler.py           # 标注主流程（模型无关）
│   │   ├── adapters/            # 多模型适配器（核心抽象层）
│   │   │   ├── __init__.py
│   │   │   ├── base.py          # 适配器基类
│   │   │   ├── claude_adapter.py
│   │   │   ├── kimi_adapter.py
│   │   │   ├── openai_adapter.py
│   │   │   └── gemini_adapter.py
│   │   ├── parser.py            # 响应解析器（JSON → Detection）
│   │   └── converter.py         # Detection → YOLO格式转换
│   │
│   ├── quality/                 # Step 3: 质量验证模块
│   │   ├── __init__.py
│   │   ├── visualizer.py        # 标注可视化
│   │   ├── statistics.py        # 标注统计分析
│   │   └── validator.py         # 异常检测（坐标越界等）
│   │
│   ├── dataset/                 # Step 4: 数据集构建模块
│   │   ├── __init__.py
│   │   ├── splitter.py          # 训练/验证/测试集划分
│   │   └── augmentor.py         # 数据增强（可选）
│   │
│   ├── training/                # Step 5: 模型训练模块
│   │   ├── __init__.py
│   │   ├── train_yolo.py        # YOLOv8 虫巢检测训练
│   │   ├── train_segmentation.py # DeepLabV3+ 树种识别训练
│   │   └── export.py            # 模型导出（PT / ONNX / TensorRT）
│   │
│   └── utils/
│       ├── image_utils.py       # 图片读取、缩放、编码
│       ├── exif_utils.py        # EXIF解析
│       └── file_utils.py        # 文件遍历、路径处理
│
├── scripts/                     # 命令行入口脚本
│   ├── run_labeling.py          # 运行自动标注
│   ├── run_quality_check.py     # 运行质量检查
│   ├── run_build_dataset.py     # 构建数据集
│   ├── run_training.py          # 运行训练
│   └── run_full_pipeline.py     # 一键运行全流程
│
├── data/                        # 数据目录
│   ├── raw_images/              # 原始航拍图片
│   ├── labels/                  # 自动标注输出
│   ├── quality_reports/         # 质量检查报告
│   └── dataset/                 # 构建好的数据集
│       ├── images/
│       │   ├── train/
│       │   ├── val/
│       │   └── test/
│       ├── labels/
│       │   ├── train/
│       │   ├── val/
│       │   └── test/
│       └── data.yaml            # YOLOv8 数据集配置
│
├── outputs/                     # 训练输出
│   ├── models/                  # 训练好的模型权重
│   └── reports/                 # 训练报告
│
├── requirements.txt
└── README.md
```

---

## 三、全局配置设计

### 3.1 配置文件 config/config.yaml

```yaml
# ============================================================
# 全局配置
# ============================================================

# ---------- 大模型配置（标注用） ----------
llm:
  # 当前使用的模型适配器: claude / kimi / openai / gemini
  provider: "claude"

  claude:
    model: "claude-sonnet-4-5-20250514"
    api_key_env: "ANTHROPIC_API_KEY"      # 从环境变量读取
    max_tokens: 1024
    # Batch API模式（更便宜，24小时内完成）
    use_batch: false

  kimi:
    model: "moonshot-v1-128k"
    api_key_env: "KIMI_API_KEY"
    base_url: "https://api.moonshot.cn/v1"
    max_tokens: 1024

  openai:
    model: "gpt-4o"
    api_key_env: "OPENAI_API_KEY"
    max_tokens: 1024

  gemini:
    model: "gemini-2.5-pro"
    api_key_env: "GOOGLE_API_KEY"
    max_tokens: 1024

# ---------- 标注配置 ----------
labeling:
  # 提示词模板文件路径
  prompt_template: "config/prompts/nest_detection.txt"
  # 最低置信度过滤: high / medium / low
  min_confidence: "low"
  # API调用间隔（秒），避免限速
  request_interval: 0.5
  # 最大重试次数
  max_retries: 3
  # 支持的图片格式
  supported_formats: [".jpg", ".jpeg", ".png", ".webp"]

# ---------- 类别配置 ----------
classes:
  - name: "nest"
    id: 0
    description: "樟巢螟虫巢"

# ---------- 数据集配置 ----------
dataset:
  # 划分比例
  train_ratio: 0.7
  val_ratio: 0.2
  test_ratio: 0.1
  # 随机种子（保证可复现）
  seed: 42

# ---------- 训练配置 ----------
training:
  yolo:
    model_size: "yolov8m"        # n / s / m / l / x
    pretrained: "yolov8m.pt"
    epochs: 200
    batch_size: 16
    img_size: 640
    optimizer: "SGD"
    lr0: 0.01
    lrf: 0.01                   # 最终学习率 = lr0 * lrf
    augment: true
    device: "0"                  # GPU编号，多卡用 "0,1"
  
  segmentation:
    model: "deeplabv3_resnet101"
    pretrained: true
    epochs: 100
    batch_size: 16
    img_size: 512
    lr: 0.001
    optimizer: "AdamW"

# ---------- 路径配置 ----------
paths:
  raw_images: "data/raw_images"
  labels: "data/labels"
  quality_reports: "data/quality_reports"
  dataset: "data/dataset"
  models: "outputs/models"
  reports: "outputs/reports"
```

---

## 四、Step 2 详细设计：AI自动标注模块

这是整个管线的核心模块，设计重点是**模型无关性**——通过适配器模式隔离不同大模型的差异，切换模型只需修改配置，不改业务代码。

### 4.1 架构设计

```
┌─────────────────────────────────────────────────┐
│                  labeler.py                      │
│              (标注主流程编排)                      │
│                                                  │
│  遍历图片 → 调用适配器 → 解析响应 → 转YOLO → 保存  │
└────────────────────┬────────────────────────────┘
                     │ 调用
                     ▼
┌─────────────────────────────────────────────────┐
│              adapters/base.py                    │
│           (BaseLLMAdapter 基类)                   │
│                                                  │
│  定义统一接口: detect_nests(image_path) → JSON    │
└────────────────────┬────────────────────────────┘
                     │ 继承
        ┌────────────┼────────────┬────────────┐
        ▼            ▼            ▼            ▼
   ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
   │ Claude  │ │  Kimi   │ │ OpenAI  │ │ Gemini  │
   │ Adapter │ │ Adapter │ │ Adapter │ │ Adapter │
   └─────────┘ └─────────┘ └─────────┘ └─────────┘
```

### 4.2 适配器基类

```python
# src/labeling/adapters/base.py

from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import Optional
import base64
from pathlib import Path


@dataclass
class Detection:
    """单个虫巢检测结果"""
    x_center: float        # 0~1, 相对图片宽度
    y_center: float        # 0~1, 相对图片高度
    width: float           # 0~1
    height: float          # 0~1
    severity: str = "medium"    # light / medium / severe
    confidence: str = "medium"  # high / medium / low
    class_id: int = 0

    def is_valid(self) -> bool:
        """坐标合法性检查"""
        return (
            0 <= self.x_center <= 1
            and 0 <= self.y_center <= 1
            and 0 < self.width <= 1
            and 0 < self.height <= 1
            and self.x_center - self.width / 2 >= -0.01
            and self.x_center + self.width / 2 <= 1.01
            and self.y_center - self.height / 2 >= -0.01
            and self.y_center + self.height / 2 <= 1.01
        )

    def to_yolo_line(self) -> str:
        """转为YOLO格式: class_id x_center y_center width height"""
        return f"{self.class_id} {self.x_center:.6f} {self.y_center:.6f} {self.width:.6f} {self.height:.6f}"


@dataclass
class LabelingResult:
    """单张图片的标注结果"""
    image_path: str
    has_camphor_tree: bool = False
    detections: list = field(default_factory=list)  # List[Detection]
    summary: str = ""
    error: Optional[str] = None
    raw_response: str = ""
    token_input: int = 0
    token_output: int = 0


class BaseLLMAdapter(ABC):
    """
    大模型适配器基类。
    
    所有适配器必须实现 detect_nests() 方法，
    接收图片路径，返回统一的 LabelingResult。
    
    新增适配器只需:
    1. 继承此类
    2. 实现 detect_nests() 方法
    3. 在 adapter_factory() 中注册
    """

    def __init__(self, config: dict):
        """
        参数:
            config: 该适配器对应的配置字典
                    如 config['llm']['claude'] 的内容
        """
        self.config = config

    @abstractmethod
    def detect_nests(self, image_path: str, prompt: str) -> LabelingResult:
        """
        调用大模型分析图片，检测虫巢。
        
        参数:
            image_path: 图片文件路径
            prompt: 用户提示词（描述任务和输出格式）
        
        返回:
            LabelingResult 统一结构
        """
        pass

    def encode_image_base64(self, image_path: str) -> tuple[str, str]:
        """将图片编码为base64，返回 (data, media_type)"""
        path = Path(image_path)
        media_map = {
            ".jpg": "image/jpeg", ".jpeg": "image/jpeg",
            ".png": "image/png", ".webp": "image/webp",
        }
        media_type = media_map.get(path.suffix.lower(), "image/jpeg")
        with open(image_path, "rb") as f:
            data = base64.standard_b64encode(f.read()).decode("utf-8")
        return data, media_type


def adapter_factory(provider: str, config: dict) -> BaseLLMAdapter:
    """
    适配器工厂函数。根据 provider 名称返回对应的适配器实例。
    
    参数:
        provider: "claude" / "kimi" / "openai" / "gemini"
        config: 对应模型的配置字典
    """
    from .claude_adapter import ClaudeAdapter
    from .kimi_adapter import KimiAdapter
    from .openai_adapter import OpenAIAdapter
    from .gemini_adapter import GeminiAdapter

    adapters = {
        "claude": ClaudeAdapter,
        "kimi": KimiAdapter,
        "openai": OpenAIAdapter,
        "gemini": GeminiAdapter,
    }

    adapter_class = adapters.get(provider)
    if not adapter_class:
        raise ValueError(
            f"不支持的模型: {provider}。支持: {list(adapters.keys())}"
        )
    return adapter_class(config)
```

### 4.3 Claude适配器实现

```python
# src/labeling/adapters/claude_adapter.py

import os
import anthropic
from .base import BaseLLMAdapter, LabelingResult


class ClaudeAdapter(BaseLLMAdapter):
    """Anthropic Claude 适配器"""

    def __init__(self, config: dict):
        super().__init__(config)
        api_key = os.environ.get(config.get("api_key_env", "ANTHROPIC_API_KEY"))
        if not api_key:
            raise ValueError("未设置 ANTHROPIC_API_KEY 环境变量")
        self.client = anthropic.Anthropic(api_key=api_key)
        self.model = config.get("model", "claude-sonnet-4-5-20250514")
        self.max_tokens = config.get("max_tokens", 1024)

    def detect_nests(self, image_path: str, prompt: str) -> LabelingResult:
        result = LabelingResult(image_path=image_path)
        try:
            image_data, media_type = self.encode_image_base64(image_path)

            response = self.client.messages.create(
                model=self.model,
                max_tokens=self.max_tokens,
                system=self._get_system_prompt(),
                messages=[{
                    "role": "user",
                    "content": [
                        {
                            "type": "image",
                            "source": {
                                "type": "base64",
                                "media_type": media_type,
                                "data": image_data,
                            },
                        },
                        {"type": "text", "text": prompt},
                    ],
                }],
            )

            result.raw_response = response.content[0].text
            result.token_input = response.usage.input_tokens
            result.token_output = response.usage.output_tokens

        except anthropic.RateLimitError as e:
            result.error = f"API限速: {e}"
        except anthropic.APIError as e:
            result.error = f"API错误: {e}"
        except Exception as e:
            result.error = f"未知错误: {e}"

        return result

    def _get_system_prompt(self) -> str:
        return (
            "你是一个专业的林业病虫害检测AI助手，"
            "专门识别无人机航拍图像中的樟巢螟虫巢。"
            "请严格按照用户要求的JSON格式返回结果。"
        )
```

### 4.4 Kimi适配器实现

```python
# src/labeling/adapters/kimi_adapter.py

import os
import json
import httpx
from .base import BaseLLMAdapter, LabelingResult


class KimiAdapter(BaseLLMAdapter):
    """Moonshot Kimi 适配器（兼容 OpenAI 接口格式）"""

    def __init__(self, config: dict):
        super().__init__(config)
        api_key = os.environ.get(config.get("api_key_env", "KIMI_API_KEY"))
        if not api_key:
            raise ValueError("未设置 KIMI_API_KEY 环境变量")
        self.api_key = api_key
        self.model = config.get("model", "moonshot-v1-128k")
        self.base_url = config.get("base_url", "https://api.moonshot.cn/v1")
        self.max_tokens = config.get("max_tokens", 1024)

    def detect_nests(self, image_path: str, prompt: str) -> LabelingResult:
        result = LabelingResult(image_path=image_path)
        try:
            image_data, media_type = self.encode_image_base64(image_path)

            headers = {
                "Authorization": f"Bearer {self.api_key}",
                "Content-Type": "application/json",
            }
            payload = {
                "model": self.model,
                "max_tokens": self.max_tokens,
                "messages": [
                    {
                        "role": "system",
                        "content": (
                            "你是一个专业的林业病虫害检测AI助手，"
                            "专门识别无人机航拍图像中的樟巢螟虫巢。"
                        ),
                    },
                    {
                        "role": "user",
                        "content": [
                            {
                                "type": "image_url",
                                "image_url": {
                                    "url": f"data:{media_type};base64,{image_data}"
                                },
                            },
                            {"type": "text", "text": prompt},
                        ],
                    },
                ],
            }

            with httpx.Client(timeout=60) as client:
                resp = client.post(
                    f"{self.base_url}/chat/completions",
                    headers=headers,
                    json=payload,
                )
                resp.raise_for_status()
                data = resp.json()

            result.raw_response = data["choices"][0]["message"]["content"]
            usage = data.get("usage", {})
            result.token_input = usage.get("prompt_tokens", 0)
            result.token_output = usage.get("completion_tokens", 0)

        except httpx.HTTPStatusError as e:
            result.error = f"HTTP错误 {e.response.status_code}: {e.response.text[:200]}"
        except Exception as e:
            result.error = f"未知错误: {e}"

        return result
```

### 4.5 OpenAI适配器实现

```python
# src/labeling/adapters/openai_adapter.py

import os
from openai import OpenAI
from .base import BaseLLMAdapter, LabelingResult


class OpenAIAdapter(BaseLLMAdapter):
    """OpenAI GPT-4o 适配器"""

    def __init__(self, config: dict):
        super().__init__(config)
        api_key = os.environ.get(config.get("api_key_env", "OPENAI_API_KEY"))
        if not api_key:
            raise ValueError("未设置 OPENAI_API_KEY 环境变量")
        self.client = OpenAI(api_key=api_key)
        self.model = config.get("model", "gpt-4o")
        self.max_tokens = config.get("max_tokens", 1024)

    def detect_nests(self, image_path: str, prompt: str) -> LabelingResult:
        result = LabelingResult(image_path=image_path)
        try:
            image_data, media_type = self.encode_image_base64(image_path)

            response = self.client.chat.completions.create(
                model=self.model,
                max_tokens=self.max_tokens,
                messages=[
                    {
                        "role": "system",
                        "content": (
                            "你是一个专业的林业病虫害检测AI助手，"
                            "专门识别无人机航拍图像中的樟巢螟虫巢。"
                        ),
                    },
                    {
                        "role": "user",
                        "content": [
                            {
                                "type": "image_url",
                                "image_url": {
                                    "url": f"data:{media_type};base64,{image_data}"
                                },
                            },
                            {"type": "text", "text": prompt},
                        ],
                    },
                ],
            )

            result.raw_response = response.choices[0].message.content
            result.token_input = response.usage.prompt_tokens
            result.token_output = response.usage.completion_tokens

        except Exception as e:
            result.error = f"OpenAI错误: {e}"

        return result
```

### 4.6 Gemini适配器实现

```python
# src/labeling/adapters/gemini_adapter.py

import os
import json
import httpx
from .base import BaseLLMAdapter, LabelingResult


class GeminiAdapter(BaseLLMAdapter):
    """Google Gemini 适配器"""

    def __init__(self, config: dict):
        super().__init__(config)
        api_key = os.environ.get(config.get("api_key_env", "GOOGLE_API_KEY"))
        if not api_key:
            raise ValueError("未设置 GOOGLE_API_KEY 环境变量")
        self.api_key = api_key
        self.model = config.get("model", "gemini-2.5-pro")
        self.max_tokens = config.get("max_tokens", 1024)

    def detect_nests(self, image_path: str, prompt: str) -> LabelingResult:
        result = LabelingResult(image_path=image_path)
        try:
            image_data, media_type = self.encode_image_base64(image_path)

            url = (
                f"https://generativelanguage.googleapis.com/v1beta/"
                f"models/{self.model}:generateContent?key={self.api_key}"
            )
            payload = {
                "contents": [{
                    "parts": [
                        {
                            "inline_data": {
                                "mime_type": media_type,
                                "data": image_data,
                            }
                        },
                        {"text": prompt},
                    ]
                }],
                "generationConfig": {
                    "maxOutputTokens": self.max_tokens,
                },
            }

            with httpx.Client(timeout=60) as client:
                resp = client.post(url, json=payload)
                resp.raise_for_status()
                data = resp.json()

            result.raw_response = (
                data["candidates"][0]["content"]["parts"][0]["text"]
            )
            usage = data.get("usageMetadata", {})
            result.token_input = usage.get("promptTokenCount", 0)
            result.token_output = usage.get("candidatesTokenCount", 0)

        except Exception as e:
            result.error = f"Gemini错误: {e}"

        return result
```

### 4.7 响应解析器

所有适配器返回的 `raw_response` 都是JSON字符串，由统一的解析器转换为 `Detection` 对象列表。

```python
# src/labeling/parser.py

import json
import logging
from .adapters.base import Detection, LabelingResult

logger = logging.getLogger(__name__)

# 置信度数值映射
CONFIDENCE_LEVELS = {"high": 3, "medium": 2, "low": 1}


def parse_response(result: LabelingResult, min_confidence: str = "low") -> LabelingResult:
    """
    解析大模型返回的JSON，提取检测结果填充到 LabelingResult。
    
    此函数与具体的大模型无关，只处理统一的JSON格式。
    
    参数:
        result: 包含 raw_response 的 LabelingResult
        min_confidence: 最低置信度过滤阈值
    
    返回:
        填充了 detections 的 LabelingResult
    """
    if result.error or not result.raw_response:
        return result

    min_conf_level = CONFIDENCE_LEVELS.get(min_confidence, 1)

    try:
        text = result.raw_response.strip()

        # 处理可能的 markdown 代码块包裹
        if text.startswith("```"):
            # 去掉首行 ```json 或 ```
            first_newline = text.find("\n")
            if first_newline != -1:
                text = text[first_newline + 1:]
            else:
                text = text[3:]
        if text.endswith("```"):
            text = text[:-3]
        text = text.strip()

        data = json.loads(text)

        # 解析基础字段
        result.has_camphor_tree = data.get("image_has_camphor_tree", False)
        result.summary = data.get("summary", "")

        # 解析检测结果
        for det_data in data.get("detections", []):
            detection = Detection(
                x_center=float(det_data["x_center"]),
                y_center=float(det_data["y_center"]),
                width=float(det_data["width"]),
                height=float(det_data["height"]),
                severity=det_data.get("severity", "medium"),
                confidence=det_data.get("confidence", "medium"),
                class_id=0,  # nest
            )

            # 坐标合法性校验
            if not detection.is_valid():
                logger.warning(
                    f"坐标越界，跳过: "
                    f"({detection.x_center:.3f}, {detection.y_center:.3f})"
                )
                continue

            # 置信度过滤
            conf_level = CONFIDENCE_LEVELS.get(detection.confidence, 1)
            if conf_level < min_conf_level:
                continue

            result.detections.append(detection)

    except json.JSONDecodeError as e:
        result.error = f"JSON解析失败: {e}"
        logger.error(f"JSON解析失败: {result.raw_response[:200]}")
    except (KeyError, ValueError, TypeError) as e:
        result.error = f"数据格式错误: {e}"

    return result
```

### 4.8 YOLO格式转换器

```python
# src/labeling/converter.py

from pathlib import Path
from .adapters.base import LabelingResult


def save_yolo_label(result: LabelingResult, output_dir: str) -> str:
    """
    将标注结果保存为YOLO格式的 .txt 文件。
    
    YOLO格式: 每行一个目标
    class_id x_center y_center width height
    
    参数:
        result: 标注结果
        output_dir: 标注文件输出目录
    
    返回:
        保存的标注文件路径
    """
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)

    image_stem = Path(result.image_path).stem
    label_path = output_dir / f"{image_stem}.txt"

    with open(label_path, "w") as f:
        for det in result.detections:
            f.write(det.to_yolo_line() + "\n")

    # 即使无检测结果也写空文件（YOLO需要）
    return str(label_path)


def generate_classes_file(output_dir: str, classes: list[dict]) -> str:
    """
    生成 classes.txt 文件。
    
    参数:
        output_dir: 输出目录
        classes: 类别列表 [{"id": 0, "name": "nest"}, ...]
    """
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)
    
    classes_path = output_dir / "classes.txt"
    sorted_classes = sorted(classes, key=lambda c: c["id"])
    
    with open(classes_path, "w") as f:
        for cls in sorted_classes:
            f.write(cls["name"] + "\n")
    
    return str(classes_path)
```

### 4.9 标注主流程编排

```python
# src/labeling/labeler.py

import time
import logging
from pathlib import Path

from .adapters.base import adapter_factory, LabelingResult
from .parser import parse_response
from .converter import save_yolo_label, generate_classes_file

logger = logging.getLogger(__name__)


class AutoLabeler:
    """
    自动标注主流程。
    
    职责:
    1. 遍历图片目录
    2. 调用大模型适配器获取检测结果
    3. 解析响应并验证
    4. 保存为YOLO格式标注文件
    5. 生成统计报告
    """

    def __init__(self, config: dict):
        self.config = config
        llm_config = config["llm"]
        provider = llm_config["provider"]
        provider_config = llm_config[provider]

        # 创建适配器（可替换，由配置决定）
        self.adapter = adapter_factory(provider, provider_config)
        self.provider = provider

        # 标注配置
        label_config = config.get("labeling", {})
        self.min_confidence = label_config.get("min_confidence", "low")
        self.request_interval = label_config.get("request_interval", 0.5)
        self.max_retries = label_config.get("max_retries", 3)
        self.supported_formats = set(
            label_config.get("supported_formats", [".jpg", ".jpeg", ".png"])
        )

        # 加载提示词模板
        prompt_path = label_config.get("prompt_template")
        if prompt_path and Path(prompt_path).exists():
            self.prompt = Path(prompt_path).read_text(encoding="utf-8")
        else:
            self.prompt = self._default_prompt()

    def run(self, input_dir: str, output_dir: str) -> list[LabelingResult]:
        """
        运行批量标注。
        
        参数:
            input_dir: 图片目录
            output_dir: 标注输出目录
        
        返回:
            所有图片的标注结果列表
        """
        input_dir = Path(input_dir)
        output_dir = Path(output_dir)
        output_dir.mkdir(parents=True, exist_ok=True)

        # 收集图片
        images = sorted([
            f for f in input_dir.iterdir()
            if f.suffix.lower() in self.supported_formats
        ])

        if not images:
            logger.error(f"目录中未找到图片: {input_dir}")
            return []

        logger.info(f"使用模型: {self.provider}")
        logger.info(f"找到 {len(images)} 张图片，开始标注...")

        results = []
        stats = {"total": len(images), "success": 0, "error": 0,
                 "with_nest": 0, "total_nests": 0,
                 "total_input_tokens": 0, "total_output_tokens": 0}

        for i, img_path in enumerate(images, 1):
            logger.info(f"[{i}/{len(images)}] {img_path.name}")

            # 调用适配器（含重试）
            result = self._call_with_retry(str(img_path))

            # 解析响应
            if not result.error:
                result = parse_response(result, self.min_confidence)

            # 保存标注
            if not result.error:
                save_yolo_label(result, str(output_dir))
                stats["success"] += 1
                if result.detections:
                    stats["with_nest"] += 1
                    stats["total_nests"] += len(result.detections)
                logger.info(f"  → {len(result.detections)} 个虫巢")
            else:
                stats["error"] += 1
                logger.error(f"  ✗ {result.error}")

            stats["total_input_tokens"] += result.token_input
            stats["total_output_tokens"] += result.token_output
            results.append(result)

            # 请求间隔
            if i < len(images):
                time.sleep(self.request_interval)

        # 生成类别文件
        generate_classes_file(str(output_dir), self.config.get("classes", []))

        # 打印统计
        self._print_stats(stats)

        # 保存标注报告
        self._save_report(results, stats, str(output_dir))

        return results

    def _call_with_retry(self, image_path: str) -> LabelingResult:
        """带重试的API调用"""
        for attempt in range(self.max_retries):
            result = self.adapter.detect_nests(image_path, self.prompt)
            if not result.error:
                return result
            if attempt < self.max_retries - 1:
                wait = self.request_interval * (2 ** attempt)
                logger.warning(f"  重试 {attempt+1}/{self.max_retries}，等待 {wait}s")
                time.sleep(wait)
        return result

    def _print_stats(self, stats: dict):
        """打印统计信息"""
        logger.info("\n" + "=" * 50)
        logger.info(f"标注完成 | 模型: {self.provider}")
        logger.info("=" * 50)
        logger.info(f"总图片数:    {stats['total']}")
        logger.info(f"成功标注:    {stats['success']}")
        logger.info(f"标注失败:    {stats['error']}")
        logger.info(f"含虫巢图片:  {stats['with_nest']}")
        logger.info(f"总虫巢数:    {stats['total_nests']}")
        logger.info(f"输入tokens:  {stats['total_input_tokens']:,}")
        logger.info(f"输出tokens:  {stats['total_output_tokens']:,}")

    def _save_report(self, results, stats, output_dir):
        """保存JSON格式的标注报告"""
        import json
        report = {
            "provider": self.provider,
            "stats": stats,
            "details": [
                {
                    "image": Path(r.image_path).name,
                    "nest_count": len(r.detections),
                    "has_camphor_tree": r.has_camphor_tree,
                    "summary": r.summary,
                    "error": r.error,
                }
                for r in results
            ],
        }
        report_path = Path(output_dir) / "labeling_report.json"
        with open(report_path, "w", encoding="utf-8") as f:
            json.dump(report, f, ensure_ascii=False, indent=2)

    def _default_prompt(self) -> str:
        """默认提示词（当配置文件中的模板不存在时使用）"""
        return """请仔细分析这张无人机航拍图像，检测图中所有的樟巢螟虫巢。

樟巢螟虫巢的特征：
1. 颜色：棕褐色/暗褐色，与周围绿色树叶形成强烈对比
2. 形态：叶片卷缩、枯萎、缀叶成团，不规则团块状
3. 纹理：虫巢表面可见丝网结构
4. 位置：通常在树冠中上部

对于每个检测到的虫巢，返回：
- x_center, y_center: 中心点相对坐标 (0~1)，左上角为(0,0)
- width, height: 边界框相对宽高 (0~1)
- severity: 严重程度 "light" / "medium" / "severe"
- confidence: 置信度 "high" / "medium" / "low"

严格按以下JSON格式回复，不要有其他文字：
{
  "image_has_camphor_tree": true,
  "detections": [
    {
      "x_center": 0.55,
      "y_center": 0.42,
      "width": 0.08,
      "height": 0.06,
      "severity": "medium",
      "confidence": "high"
    }
  ],
  "summary": "简要描述"
}

如果没有检测到虫巢:
{
  "image_has_camphor_tree": false,
  "detections": [],
  "summary": "未检测到虫巢"
}"""
```

---

## 五、Step 3 详细设计：标注质量验证

### 5.1 可视化检查

```python
# src/quality/visualizer.py

from pathlib import Path
from PIL import Image, ImageDraw, ImageFont

# 严重程度对应的颜色
SEVERITY_COLORS = {
    "light": "#4CAF50",    # 绿色
    "medium": "#FF9800",   # 橙色
    "severe": "#F44336",   # 红色
}


def visualize_labels(image_path: str, label_path: str, output_path: str):
    """
    在图片上绘制YOLO标注框，用于人工目视检查。
    
    参数:
        image_path: 原始图片路径
        label_path: YOLO格式标注文件路径
        output_path: 输出图片路径
    """
    img = Image.open(image_path)
    draw = ImageDraw.Draw(img)
    w, h = img.size

    with open(label_path, "r") as f:
        lines = f.readlines()

    for line in lines:
        parts = line.strip().split()
        if len(parts) < 5:
            continue

        cls_id = int(parts[0])
        xc, yc, bw, bh = [float(x) for x in parts[1:5]]

        # 转为像素坐标
        x1 = int((xc - bw / 2) * w)
        y1 = int((yc - bh / 2) * h)
        x2 = int((xc + bw / 2) * w)
        y2 = int((yc + bh / 2) * h)

        color = "#F44336"
        draw.rectangle([x1, y1, x2, y2], outline=color, width=3)
        draw.text((x1, y1 - 15), f"nest", fill=color)

    img.save(output_path)


def batch_visualize(image_dir: str, label_dir: str, output_dir: str,
                    sample_count: int = 50):
    """
    批量生成可视化图片（随机抽样）。
    
    参数:
        image_dir: 原始图片目录
        label_dir: 标注文件目录
        output_dir: 可视化输出目录
        sample_count: 抽样数量
    """
    import random

    image_dir = Path(image_dir)
    label_dir = Path(label_dir)
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)

    # 找到有标注的图片
    label_files = list(label_dir.glob("*.txt"))
    # 优先抽取有检测结果的
    non_empty = [f for f in label_files if f.stat().st_size > 0]
    empty = [f for f in label_files if f.stat().st_size == 0]

    # 有检测结果的全部可视化，空的随机抽一些
    samples = non_empty[:sample_count]
    remaining = sample_count - len(samples)
    if remaining > 0 and empty:
        samples.extend(random.sample(empty, min(remaining, len(empty))))

    for label_file in samples:
        stem = label_file.stem
        # 查找对应的图片
        for ext in [".jpg", ".jpeg", ".png", ".webp"]:
            img_path = image_dir / f"{stem}{ext}"
            if img_path.exists():
                vis_path = output_dir / f"{stem}_vis.jpg"
                visualize_labels(str(img_path), str(label_file), str(vis_path))
                break
```

### 5.2 统计分析

```python
# src/quality/statistics.py

import json
from pathlib import Path
from collections import Counter


def generate_statistics(label_dir: str, report_path: str = None) -> dict:
    """
    统计标注数据的分布情况。
    
    返回:
        {
            "total_images": 1000,
            "images_with_nests": 156,
            "images_without_nests": 844,
            "total_nests": 203,
            "nests_per_image_distribution": {0: 844, 1: 120, 2: 30, 3: 6},
            "avg_bbox_width": 0.065,
            "avg_bbox_height": 0.052,
        }
    """
    label_dir = Path(label_dir)
    label_files = list(label_dir.glob("*.txt"))

    stats = {
        "total_images": len(label_files),
        "images_with_nests": 0,
        "images_without_nests": 0,
        "total_nests": 0,
        "nests_per_image": Counter(),
        "bbox_widths": [],
        "bbox_heights": [],
    }

    for label_file in label_files:
        lines = label_file.read_text().strip().split("\n")
        lines = [l for l in lines if l.strip()]
        count = len(lines)

        stats["nests_per_image"][count] += 1

        if count > 0:
            stats["images_with_nests"] += 1
            stats["total_nests"] += count
            for line in lines:
                parts = line.split()
                if len(parts) >= 5:
                    stats["bbox_widths"].append(float(parts[3]))
                    stats["bbox_heights"].append(float(parts[4]))
        else:
            stats["images_without_nests"] += 1

    # 计算统计值
    result = {
        "total_images": stats["total_images"],
        "images_with_nests": stats["images_with_nests"],
        "images_without_nests": stats["images_without_nests"],
        "nest_positive_rate": (
            stats["images_with_nests"] / max(stats["total_images"], 1)
        ),
        "total_nests": stats["total_nests"],
        "avg_nests_per_positive_image": (
            stats["total_nests"] / max(stats["images_with_nests"], 1)
        ),
        "nests_per_image_distribution": dict(
            sorted(stats["nests_per_image"].items())
        ),
        "avg_bbox_width": (
            sum(stats["bbox_widths"]) / max(len(stats["bbox_widths"]), 1)
        ),
        "avg_bbox_height": (
            sum(stats["bbox_heights"]) / max(len(stats["bbox_heights"]), 1)
        ),
    }

    if report_path:
        with open(report_path, "w", encoding="utf-8") as f:
            json.dump(result, f, ensure_ascii=False, indent=2)

    return result
```

---

## 六、Step 4 详细设计：数据集构建

```python
# src/dataset/splitter.py

import shutil
import random
from pathlib import Path


def build_yolo_dataset(
    image_dir: str,
    label_dir: str,
    output_dir: str,
    train_ratio: float = 0.7,
    val_ratio: float = 0.2,
    test_ratio: float = 0.1,
    seed: int = 42,
):
    """
    将图片和标注文件划分为训练/验证/测试集，
    生成 YOLOv8 所需的目录结构和 data.yaml 配置文件。
    
    输出目录结构:
        output_dir/
        ├── images/
        │   ├── train/
        │   ├── val/
        │   └── test/
        ├── labels/
        │   ├── train/
        │   ├── val/
        │   └── test/
        └── data.yaml
    """
    image_dir = Path(image_dir)
    label_dir = Path(label_dir)
    output_dir = Path(output_dir)

    # 创建目录
    for split in ["train", "val", "test"]:
        (output_dir / "images" / split).mkdir(parents=True, exist_ok=True)
        (output_dir / "labels" / split).mkdir(parents=True, exist_ok=True)

    # 收集有标注的图片
    pairs = []
    for label_file in label_dir.glob("*.txt"):
        if label_file.name == "classes.txt":
            continue
        stem = label_file.stem
        # 查找对应图片
        for ext in [".jpg", ".jpeg", ".png", ".webp"]:
            img_path = image_dir / f"{stem}{ext}"
            if img_path.exists():
                pairs.append((img_path, label_file))
                break

    # 打乱并划分
    random.seed(seed)
    random.shuffle(pairs)

    n = len(pairs)
    n_train = int(n * train_ratio)
    n_val = int(n * val_ratio)

    splits = {
        "train": pairs[:n_train],
        "val": pairs[n_train:n_train + n_val],
        "test": pairs[n_train + n_val:],
    }

    # 复制文件
    for split_name, split_pairs in splits.items():
        for img_path, label_path in split_pairs:
            shutil.copy2(img_path, output_dir / "images" / split_name / img_path.name)
            shutil.copy2(label_path, output_dir / "labels" / split_name / label_path.name)

    # 生成 data.yaml
    data_yaml = {
        "path": str(output_dir.resolve()),
        "train": "images/train",
        "val": "images/val",
        "test": "images/test",
        "names": {0: "nest"},
    }

    import yaml
    yaml_path = output_dir / "data.yaml"
    with open(yaml_path, "w") as f:
        yaml.dump(data_yaml, f, default_flow_style=False)

    print(f"数据集构建完成:")
    print(f"  训练集: {len(splits['train'])} 张")
    print(f"  验证集: {len(splits['val'])} 张")
    print(f"  测试集: {len(splits['test'])} 张")
    print(f"  配置文件: {yaml_path}")

    return str(yaml_path)
```

---

## 七、Step 5 详细设计：模型训练

### 7.1 YOLOv8 虫巢检测训练

```python
# src/training/train_yolo.py

from ultralytics import YOLO
from pathlib import Path


def train_nest_detector(
    data_yaml: str,
    model_size: str = "yolov8m",
    pretrained: str = "yolov8m.pt",
    epochs: int = 200,
    batch_size: int = 16,
    img_size: int = 640,
    device: str = "0",
    output_dir: str = "outputs/models",
    **kwargs,
):
    """
    训练 YOLOv8 虫巢检测模型。
    
    参数:
        data_yaml: 数据集配置文件路径 (data.yaml)
        model_size: 模型大小 n/s/m/l/x
        pretrained: 预训练权重路径
        epochs: 训练轮数
        batch_size: 批大小
        img_size: 输入图像尺寸
        device: GPU设备编号
        output_dir: 输出目录
    
    返回:
        最佳模型权重路径
    """
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)

    # 加载预训练模型
    model = YOLO(pretrained)

    # 开始训练
    results = model.train(
        data=data_yaml,
        epochs=epochs,
        batch=batch_size,
        imgsz=img_size,
        device=device,
        project=str(output_dir),
        name="nest_detector",
        # 优化器配置
        optimizer=kwargs.get("optimizer", "SGD"),
        lr0=kwargs.get("lr0", 0.01),
        lrf=kwargs.get("lrf", 0.01),
        # 数据增强
        augment=kwargs.get("augment", True),
        mosaic=1.0,
        mixup=0.1,
        flipud=0.5,
        fliplr=0.5,
        degrees=15.0,
        hsv_h=0.015,
        hsv_s=0.7,
        hsv_v=0.4,
        # 其他
        patience=30,          # 早停耐心值
        save_period=10,       # 每10个epoch保存
        plots=True,           # 生成训练曲线图
        verbose=True,
    )

    # 返回最佳权重路径
    best_path = output_dir / "nest_detector" / "weights" / "best.pt"
    return str(best_path)


def evaluate_model(model_path: str, data_yaml: str, device: str = "0") -> dict:
    """
    在测试集上评估模型性能。
    
    返回:
        {"mAP50": 0.87, "mAP50-95": 0.62, "precision": 0.91, "recall": 0.83}
    """
    model = YOLO(model_path)
    metrics = model.val(data=data_yaml, device=device, split="test")

    return {
        "mAP50": float(metrics.box.map50),
        "mAP50-95": float(metrics.box.map),
        "precision": float(metrics.box.mp),
        "recall": float(metrics.box.mr),
    }
```

### 7.2 模型导出

```python
# src/training/export.py

from ultralytics import YOLO


def export_model(
    model_path: str,
    formats: list[str] = None,
    output_dir: str = "outputs/models",
):
    """
    将训练好的模型导出为多种格式。
    
    参数:
        model_path: .pt 模型路径
        formats: 导出格式列表，如 ["onnx", "engine"]
        output_dir: 输出目录
    """
    if formats is None:
        formats = ["onnx"]

    model = YOLO(model_path)

    exported_files = {}
    for fmt in formats:
        path = model.export(format=fmt)
        exported_files[fmt] = str(path)
        print(f"已导出 {fmt}: {path}")

    return exported_files
```

---

## 八、命令行入口脚本

### 8.1 运行自动标注

```python
# scripts/run_labeling.py

import argparse
import logging
import yaml
from src.labeling.labeler import AutoLabeler


def main():
    parser = argparse.ArgumentParser(description="樟巢螟虫巢自动标注")
    parser.add_argument("--config", default="config/config.yaml", help="配置文件路径")
    parser.add_argument("--input", required=True, help="图片目录")
    parser.add_argument("--output", default=None, help="标注输出目录")
    parser.add_argument("--provider", default=None,
                        help="覆盖配置中的模型: claude/kimi/openai/gemini")
    parser.add_argument("--verbose", action="store_true")
    args = parser.parse_args()

    logging.basicConfig(
        level=logging.DEBUG if args.verbose else logging.INFO,
        format="%(asctime)s [%(levelname)s] %(message)s",
        datefmt="%H:%M:%S",
    )

    with open(args.config, "r") as f:
        config = yaml.safe_load(f)

    # 命令行参数覆盖配置
    if args.provider:
        config["llm"]["provider"] = args.provider

    output_dir = args.output or config["paths"]["labels"]

    labeler = AutoLabeler(config)
    labeler.run(args.input, output_dir)


if __name__ == "__main__":
    main()
```

### 8.2 一键运行全流程

```python
# scripts/run_full_pipeline.py

"""
一键运行完整管线:
  自动标注 → 质量验证 → 数据集构建 → 模型训练 → 模型评估
"""

import argparse
import logging
import yaml
from pathlib import Path

from src.labeling.labeler import AutoLabeler
from src.quality.visualizer import batch_visualize
from src.quality.statistics import generate_statistics
from src.dataset.splitter import build_yolo_dataset
from src.training.train_yolo import train_nest_detector, evaluate_model
from src.training.export import export_model


def main():
    parser = argparse.ArgumentParser(description="一键运行完整标注→训练管线")
    parser.add_argument("--config", default="config/config.yaml")
    parser.add_argument("--input", required=True, help="原始图片目录")
    parser.add_argument("--skip-labeling", action="store_true",
                        help="跳过标注步骤（已有标注数据时使用）")
    args = parser.parse_args()

    logging.basicConfig(
        level=logging.INFO,
        format="%(asctime)s [%(levelname)s] %(message)s",
    )
    logger = logging.getLogger(__name__)

    with open(args.config, "r") as f:
        config = yaml.safe_load(f)

    paths = config["paths"]

    # ========== Step 1-2: 自动标注 ==========
    if not args.skip_labeling:
        logger.info("=" * 50)
        logger.info("Step 2: AI自动标注")
        logger.info("=" * 50)
        labeler = AutoLabeler(config)
        labeler.run(args.input, paths["labels"])
    else:
        logger.info("跳过标注步骤")

    # ========== Step 3: 质量验证 ==========
    logger.info("=" * 50)
    logger.info("Step 3: 标注质量验证")
    logger.info("=" * 50)

    stats = generate_statistics(
        paths["labels"],
        report_path=str(Path(paths["quality_reports"]) / "statistics.json"),
    )
    logger.info(f"标注统计: {stats['total_images']} 张图, "
                f"{stats['images_with_nests']} 张含虫巢, "
                f"共 {stats['total_nests']} 个虫巢")

    batch_visualize(
        args.input, paths["labels"],
        str(Path(paths["quality_reports"]) / "visualized"),
        sample_count=50,
    )

    # ========== Step 4: 数据集构建 ==========
    logger.info("=" * 50)
    logger.info("Step 4: 数据集构建")
    logger.info("=" * 50)

    ds_config = config["dataset"]
    data_yaml = build_yolo_dataset(
        image_dir=args.input,
        label_dir=paths["labels"],
        output_dir=paths["dataset"],
        train_ratio=ds_config["train_ratio"],
        val_ratio=ds_config["val_ratio"],
        test_ratio=ds_config["test_ratio"],
        seed=ds_config["seed"],
    )

    # ========== Step 5: 模型训练 ==========
    logger.info("=" * 50)
    logger.info("Step 5: YOLOv8 模型训练")
    logger.info("=" * 50)

    train_config = config["training"]["yolo"]
    best_model = train_nest_detector(
        data_yaml=data_yaml,
        output_dir=paths["models"],
        **train_config,
    )
    logger.info(f"最佳模型: {best_model}")

    # ========== Step 6: 评估与导出 ==========
    logger.info("=" * 50)
    logger.info("Step 6: 模型评估与导出")
    logger.info("=" * 50)

    metrics = evaluate_model(best_model, data_yaml)
    logger.info(f"评估结果: mAP@0.5={metrics['mAP50']:.4f}, "
                f"精确率={metrics['precision']:.4f}, "
                f"召回率={metrics['recall']:.4f}")

    export_model(best_model, formats=["onnx"])

    logger.info("=" * 50)
    logger.info("全流程完成!")
    logger.info(f"模型权重: {best_model}")
    logger.info("=" * 50)


if __name__ == "__main__":
    main()
```

---

## 九、提示词模板

### 9.1 虫巢检测提示词

```
文件: config/prompts/nest_detection.txt
```

```text
请仔细分析这张无人机航拍图像，检测图中所有的樟巢螟虫巢。

## 樟巢螟虫巢的视觉特征：
1. 颜色：呈棕褐色/暗褐色，与周围健康的绿色树叶形成强烈色差
2. 形态：叶片卷缩、枯萎、缀叶成团，形成不规则团块状结构
3. 纹理：虫巢表面可见丝网结构，与正常叶面纹理明显不同
4. 位置：通常出现在香樟树树冠的中上部
5. 大小：在航拍图中表现为局部的褐色斑块

## 注意事项：
- 仅标注确定是虫巢的目标，不确定的不要标注
- 枯枝、自然落叶、阴影、土壤裸露不是虫巢
- 每个独立的虫巢分别标注
- 如果图中没有香樟树或没有虫巢，返回空列表

## 输出要求：
对于每个虫巢，返回以下信息：
- x_center, y_center: 虫巢中心点的相对坐标 (0~1)，左上角为(0,0)，右下角为(1,1)
- width, height: 虫巢边界框的相对宽高 (0~1)
- severity: 严重程度
  - "light": 单个小虫巢，面积小
  - "medium": 中等面积或多个聚集的虫巢
  - "severe": 大面积受害，叶片大量枯死
- confidence: 你对这个检测结果的置信度
  - "high": 非常确定是虫巢
  - "medium": 较确定
  - "low": 可能是虫巢但不太确定

请严格按以下JSON格式回复，不要有任何其他文字：

{
  "image_has_camphor_tree": true,
  "detections": [
    {
      "x_center": 0.55,
      "y_center": 0.42,
      "width": 0.08,
      "height": 0.06,
      "severity": "medium",
      "confidence": "high"
    }
  ],
  "summary": "检测到X个虫巢，分布在图像Y区域"
}

如果图中没有检测到虫巢:
{
  "image_has_camphor_tree": false,
  "detections": [],
  "summary": "未检测到樟巢螟虫巢"
}
```

---

## 十、如何新增一个大模型适配器

当需要接入新的大模型（例如某个国产模型）时，只需三步：

**第一步**：在 `src/labeling/adapters/` 下新建文件，如 `new_model_adapter.py`

```python
from .base import BaseLLMAdapter, LabelingResult

class NewModelAdapter(BaseLLMAdapter):
    def __init__(self, config: dict):
        super().__init__(config)
        # 初始化你的 SDK 或 HTTP client

    def detect_nests(self, image_path: str, prompt: str) -> LabelingResult:
        result = LabelingResult(image_path=image_path)
        # 1. 编码图片: self.encode_image_base64(image_path)
        # 2. 调用你的模型API
        # 3. 将原始响应写入 result.raw_response
        # 4. 将token使用写入 result.token_input / result.token_output
        # 5. 如果出错，写入 result.error
        return result
```

**第二步**：在 `adapters/base.py` 的 `adapter_factory()` 中注册

```python
from .new_model_adapter import NewModelAdapter
adapters["new_model"] = NewModelAdapter
```

**第三步**：在 `config/config.yaml` 中添加配置

```yaml
llm:
  provider: "new_model"
  new_model:
    model: "model-name"
    api_key_env: "NEW_MODEL_API_KEY"
    base_url: "https://api.newmodel.com/v1"
    max_tokens: 1024
```

无需修改 `labeler.py`、`parser.py`、`converter.py` 等任何其他文件。

---

## 十一、依赖清单

```
# requirements.txt

# 核心
anthropic>=0.40.0       # Claude API
openai>=1.50.0          # OpenAI API
httpx>=0.27.0           # HTTP client (Kimi, Gemini)
Pillow>=10.0.0          # 图片处理
PyYAML>=6.0             # 配置文件

# 训练
ultralytics>=8.1.0      # YOLOv8
torch>=2.0.0            # PyTorch
torchvision>=0.15.0     # 树种识别模型

# 数据处理
scikit-learn>=1.3.0     # 统计分析
numpy>=1.24.0

# 可选
exifread>=3.0.0         # EXIF解析
matplotlib>=3.7.0       # 可视化图表
```

---

## 十二、使用流程速查

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 API Key
export ANTHROPIC_API_KEY="sk-xxx"       # 如果用 Claude
export KIMI_API_KEY="sk-xxx"            # 如果用 Kimi

# 3. 修改配置（选择模型、调整参数）
vim config/config.yaml

# 4. 一键运行全流程
python scripts/run_full_pipeline.py --input data/raw_images/

# 或者分步运行:

# 4a. 仅运行自动标注
python scripts/run_labeling.py --input data/raw_images/ --provider claude

# 4b. 质量检查
python scripts/run_quality_check.py

# 4c. 构建数据集
python scripts/run_build_dataset.py

# 4d. 训练模型
python scripts/run_training.py

# 5. 交付产物在 outputs/models/ 目录下
```
